@model QuanLyNhanVien.Models.BaoHiem
@{

}
@if (TempData["ThongBao"] != null)
{
    <script type="text/javascript" charset="utf-8">
                        window.onload = function () {
                            alert("@Html.Raw(@TempData["ThongBao"])");
                    };
    </script>
}
<div class="container-xxl flex-grow-1 container-p-y">
    <div class="row">
        <div class="card">
            <div class="card-body">
                <form action="/BHXH/insertBHXH" method="post">
                    <div class="forms-header text-center">
                        <h4 class="forms-title">
                            <b>Thêm mức bảo hiểm</b>
                        </h4>
                    </div>
                    <div class="forms-body">
                        <div class="form-group">
                            <label class="m-2">Mã BH</label>
                            <input asp-for="@Model.MaBh" type="text" class="form-control" required>
                        </div>
                        <div class="form-group mb-2">
                            <label class="m-2">Mức đóng bảo hiểm</label>
                            <input asp-for="@Model.MucBaoHiem" type="text" class="form-control" required>
                        </div>
                        <div class="form-group mb-2">
                            <label class="m-2">Ngày áp dụng</label>
                            <input asp-for="@Model.Tgad" type="date" class="form-control" required>
                        </div>
                    </div>
                    <div class="forms-footer d-flex justify-content-between">
                        <a href="/BHXH/BHXH" class="btn btn-secondary">Quay lại</a>
                        <input type="submit"
                               class="btn btn-primary"
                               value="Thêm" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>